import imageCheck from '../lib';
import expect from 'expect.js';

describe('image-check', function () {
    it('should have unit test!', function () {
        expect('someday').to.be.ok();
    });
});
